{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Vite App - Chrome",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/src",
      "preLaunchTask": "npm: dev",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Vite App - Edge",
      "type": "msedge",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/src",
      "preLaunchTask": "npm: dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}